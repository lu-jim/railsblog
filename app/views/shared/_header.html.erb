<nav class="navbar navbar-expand-lg navbar-light bg-light w-80 px-4">
  <div class="container-fluid">
    <a class="navbar-brand h1 sans fw-bold" href="/">Rails Blog</a>
    <% unless current_user.nil?%>
      <div class="nav-item dropdown">
        <a class="btn btn-outline-primary" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
          <%= current_user.name%>
        </a>
        <ul class="dropdown-menu dropdown-menu-end"  aria-labelledby="navbarDropdown">
          <li><%= link_to "Create New Post", new_user_post_url(author=current_user), class:"dropdown-item" %></li>
          <li><hr class="dropdown-divider"></li>
          <li><%= button_to "Log Out", destroy_user_session_path, method: :delete, class:"dropdown-item"  %></li>
        </ul>
      <div>
    <% end %>
  </div>
</nav>